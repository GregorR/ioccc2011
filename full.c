#include <sys/mman.h>
#define i int
typedef i(*f)(i*);
#define B 9<<9
#define J(nm) nm(i**a,i*b,f*F){ (*(*a)
e(i**a,i*c,f*F) J(f)); }
J(a))++; } // a_dd
J(s))--; } // s_ub
J(r)++); } // r_ight
J(l)--); } // l_eft
J(p)); *F(a); } // p_utchar
J(g))=F[1](0); } // g_etchar
#define O(x) i x;
#include "o.h"
#define O(x) J(o##x)); F[2](F[&x-&aaaaaa]); }
#include "o.h"

P(i*c){ putchar(*c); }

main(){
    i c;
    char*F,*e,b[];
    void*fs[1024]={P,getchar,q};
    F=e=mmap(0,9<<25,-1,MAP_PRIVATE|MAP_ANON,-1,0);
